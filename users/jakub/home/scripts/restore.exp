#!/usr/bin/env expect

# start tmux
spawn tmux 

# delay with sleep to compensate for tmux starting time
sleep 1

# run restore script directly
send "\002\022"

# wait until tmux restore is complete
# (things get messed up if expect client isn't attached)
sleep 1 
